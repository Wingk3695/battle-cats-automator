[project]
name = "battle-cat-automator"
version = "0.1.0"
description = "Small MaaFramework automation prototype for Battle Cats."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "MaaFw>=5.10.5",
    "Pillow>=11.0.0",
]

[tool.uv]
package = false
